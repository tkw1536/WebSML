dialog.passwd = function(){
	var $passwd = 
	$("<div>")
	.attr("title", "RPC & Passwords")
	.dialog({
		dialogClass: 'noCloseDialog',
		closeOnEscape: false
	});

	$passwd.text("Unimplemented. ");
};
